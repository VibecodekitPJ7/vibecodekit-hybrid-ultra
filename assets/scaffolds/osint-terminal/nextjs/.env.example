# Optional — flip the header "TOKEN" pill from `MISSING` to `LIVE`.
# Use whatever your downstream data source authenticates with;
# the scaffold itself only checks `process.env.HAS_TOKEN === "1"`.
HAS_TOKEN=
